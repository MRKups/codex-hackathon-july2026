{"version":"v1","id":"semver-compare","name":"Compare semantic versions","spec":"Implement CompareSemver(left, right string) (int, error) for semantic versions.\n\nAccept only MAJOR.MINOR.PATCH with optional prerelease suffix -identifier.identifier. Each core component is a non-negative decimal integer with no leading zero unless it is exactly zero. Prerelease identifiers are non-empty ASCII letters, digits, or hyphens. Compare core components numerically. A release is greater than its prerelease. Compare numeric prerelease identifiers numerically; numeric identifiers sort before non-numeric ones; otherwise compare identifiers lexicographically by ASCII. Return -1, 0, or 1. Return a non-nil error for invalid input.","signature":"func CompareSemver(left, right string) (int, error)"}
